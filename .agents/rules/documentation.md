# Rule: Documentation
- If implementation diverges from a doc in `docs/`, update that doc in the same commit and add a DECISION_LOG entry if it's an architectural change.
- Keep IMPLEMENTATION_PLAN.md's traceability matrix (§9) current — add rows for anything new, never delete history.
- Every phase report follows the template in IMPLEMENTATION_PLAN.md §10.
